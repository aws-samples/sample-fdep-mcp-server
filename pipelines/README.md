# Pipelines

Declarative YAML stage graphs selected by `ace init --pipeline <name>`.

- `default.yaml` — `intent → assess → transform → ai-native`
- `prototype-led.yaml` — alternate flow for prototyping-first engagements

Contents land in task 18.
